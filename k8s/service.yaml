apiVersion: v1
kind: Service
metadata:
  name: session-api
  namespace: session-app

spec:
  type: LoadBalancer

  selector:
    app: session-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
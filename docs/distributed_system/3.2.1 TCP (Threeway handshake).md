

# TCP does much more than just add a header.

-- It performs the 3-way handshake (before any HTTP bytes are sent).
-- It segments large application data into TCP segments.
-- It reassembles segments on the receiving side.
-- It handles retransmissions, acknowledgements, flow control, and congestion control.
# Encryption (TLS/SSL) fits between HTTP and TCP.

TLS is often called Layer 6 (Presentation Layer) in the OSI model, although in practice most engineers simply say "it's above TCP and below HTTP."
Chrome implements TLS, not the OS kernel.
TCP sees only encrypted bytes; it has no idea they are HTTPS.